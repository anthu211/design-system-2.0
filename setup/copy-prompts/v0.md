# Prevalent AI Design System — v0 Prompt

Paste before your component description on v0.dev.

---

```
Fetch ALL of these URLs fully before writing any code:
1. https://anthu211.github.io/design-system-2.0/ds/core.txt
2. https://anthu211.github.io/design-system-2.0/ds/components/charts.json

---

My request: $YOUR_REQUEST_HERE

---

Build a React + TypeScript component. Follow every rule in ds-core.txt. Use Tailwind utilities for layout and spacing, CSS custom properties for all color tokens — never hardcode hex. For charts use Recharts only (AreaChart, BarChart, LineChart) — never canvas, D3, or custom SVG functions. Use only the chart types listed in charts.json.

Do not finish until every BUILD CHECKLIST item in the fetched files is complete.
```
